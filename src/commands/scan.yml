description: >
  This command is used to run a new scan.
# What will this command do?
# Descriptions should be short, simple, and clear.

steps:
  - run:
      name: Hello Greeting
      command: bright-cli --version
